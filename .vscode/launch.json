{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Train Waste Model",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/src/train.py",
      "args": ["--data_dir", "data/train", "--epochs", "10", "--batch_size", "32", "--model_out", "models/waste_mobilenet.h5"],
      "console": "integratedTerminal"
    }
  ]
}